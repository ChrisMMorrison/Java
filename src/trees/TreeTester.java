package trees;


public class TreeTester {
	
	public static void main(String[] args) {
		
		ComparableTree<String> tree = new ComparableTree<>();
		
	/*	java.util.Scanner scanner = new java.util.Scanner(System.in);
		for(int i = 0; i < 10; i++) {
			tree.add(scanner.nextLine());
		}
		
		String[] elements = {"monkey", "monkey", "cucumber", "cucumber", "cucumber", "rick", "free", "example", "nation", "shit", "poop", "help", "poop", "poop", "poop", "poop"};
		for(int i = 0; i < elements.length; i++) {
			tree.add(elements[i]);
		}
			
		tree.printIndexed();
		
		tree.remove("free");
		System.out.print("\n \n \n \n");
		tree.printIndexed();
		
		//tree.printAll();
		//scanner.close();
		tree.printTopN(3);
		
		*/
		boolean bool = Character.isDigit('k');
		System.out.println(bool);
	}

}
